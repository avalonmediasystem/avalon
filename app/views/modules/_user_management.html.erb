<%# The containers for the links need to be defined by the calling partial %>
    <% if user_signed_in? %>
      <%= user_key %> <%= link_to "Sign out", destroy_user_session_path %>
    <% else %>
      <%= link_to "Sign in", new_user_session_path %>
    <% end %>
