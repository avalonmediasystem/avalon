<%#
Copyright 2011-2025, The Trustees of Indiana University and Northwestern
  University.  Licensed under the Apache License, Version 2.0 (the "License");
  you may not use this file except in compliance with the License.

You may obtain a copy of the License at

http://www.apache.org/licenses/LICENSE-2.0

Unless required by applicable law or agreed to in writing, software distributed
  under the License is distributed on an "AS IS" BASIS, WITHOUT WARRANTIES OR
  CONDITIONS OF ANY KIND, either express or implied. See the License for the
  specific language governing permissions and limitations under the License.
---  END LICENSE_HEADER BLOCK  ---
%>

<%# locals: (object:, items:) %>

<% @page_title = t("#{object.pluralize}.title", :application_name => application_name) %>

<% unless items.empty? %>
  <div class="row display-flex page-title-wrapper">
    <div class="col-xs-6">
      <h1 class="page-title">My <%= object.pluralize.capitalize %></h1>
    </div>
    <div class="col-xs-6 collection-btn">
      <%= link_to("<span class='btn btn-primary btn-large' data-testid='#{object}-create-#{object}-button'>Create #{object.capitalize}</span>".html_safe, send("new_admin_#{object}_path".to_sym)) unless cannot? :create, "Admin::#{object.capitalize}".constantize %>
    </div>
  </div>

  <table class='table table-striped table-responsive' id='<%= "admin-#{object.pluralize}-table" %>'>
    <thead>
      <tr>
        <th>Title</th>
        <th>Items</th>
        <th>Managers</th>
        <th>Description</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <% items.to_a.each do |item| %>
      <tr>
        <td> <strong><%= link_to item.name, send("admin_#{object}_path".to_sym, item.id), 'data-testid':"#{object}-name-table" %></strong> </td>
        <% if object == 'collection' %>
          <td>
            <%= link_to(pluralize(item.media_object_count, 'item'), search_catalog_path('f[collection_ssim][]' => item.name)) %>
            <% if item.unpublished_media_object_count > 0 %>
            <%=link_to("(#{item.unpublished_media_object_count} unpublished)", search_catalog_path('f[collection_ssim][]' => item.name, 'f[workflow_published_sim][]' => "Unpublished")) %>
            <% end %>
          </td>
          <td> <%= pluralize(item.manager_count, 'manager') %> </td>
        <% elsif object == 'unit' %>
          <td>
            <%= link_to(pluralize(item.collection_count, 'collection'), collections_path(filter: item.name)) %>
          </td>
          <td> <%= pluralize(item.admin_count, 'unit administrator') %> </td>
        <% end %>
        <td> <%= item.description&.truncate(100, separator: ' ') %> </td>
        <td class="text-end">
          <% if can?(:destroy, item) %>
          <div class="btn-group">
            <%= link_to('Delete', send("remove_admin_#{object}_path".to_sym, item.id), class: 'btn btn-danger btn-sm', 'data-testid': "#{object}-delete-#{object}-btn")%>
          </div>
          <% else %>
          &nbsp;
          <% end %>
        </td>
      </tr>
      <% end %>
    </tbody>
  </table>

<% else %>
  <div class="hero-unit">
    <h2>You don't have any <%= object.pluralize %> yet</h2>

    <% if can? :create, "Admin::#{object.capitalize}".constantize %>
    <p>Would you like to create one?</p>
    <div class="whitespace"></div>
    <p>
      <%= link_to("<span class='btn btn-primary btn-large'>Create #{object.capitalize}</span>".html_safe, send("new_admin_#{object}_path".to_sym)) %>
    </p>
    <% else %>
    <p>You'll need to be assigned to one</p>
    <% end %>

  </div>
<% end %>
