<!DOCTYPE html>

<html>
  <head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
    <title><%= yield :title %>
      <% if content_for? :title %>- <% end %>Avalon Release 0 prototype
    </title>
    <%= stylesheet_link_tag "application", media: "all" %>
    <%= yield :page_styles %>
    <%= csrf_meta_tags %>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
  </head>
  
  <body>
    <div class="container">
      <div class="row-fluid whitespace-half">
      <header class="jumbotron">
        <div class="logo span2">
          <%= image_tag "temporary_logo.png", alt: "Avalon Media System - Release 1 prototype " %>
        </div>
        
        <div class="span7">
          <h1>Avalon Media System</h1>
          <p class="lead">Release 0 prototype</p>     
        </div>   
      </header>
      </div>
      
      <%= render "modules/global_navigation" %>
            
      <%= yield :breadcrumbs %>
      
      <!-- TO DO : Relocate the breadcrumbs to be here on every page except for the
                   home page -->
      <%= render "modules/user_management" %>

      <div><%= yield %></div>
    
      <div class="row-fluid">
	      <footer class="span12">
					<ul>
						<li><a href="http://variationsonvideo.org">Avalon Media System Project Website</a> </li>
						<li><%= link_to "Contact Us", comments_path %></li>
					</ul>
	      </footer>
      </div>
    </div>
    
    <!-- With this many Javascript files performance will be much better
         if they are included here. That means the important content will
         load first; the page will not gag on trying to pull down files
         from a CDN -->
    <%= javascript_include_tag "application" %>
    <%= yield :page_scripts %>
  </body>
</html>
