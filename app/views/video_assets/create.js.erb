<%= remotipart_response do %>
	console.log('TADA');
	$('#uploader').html(
		'<legend>File Upload</legend>'
		+ '<a href="/<%= @document_fedora.parts.first.descMetadata.identifier[0]%>"><%= @document_fedora.parts.first.descMetadata.title[0]%></a>'
		+ '<span id="file_status"> (<%= @document_fedora.parts.first.descMetadata.description[0] %>)</span>'
		+ '<%= button_to("Delete", "/hydra/assets/#{@document_fedora.parts.first.pid}/file_assets/#{@document_fedora.parts.first.pid}?container_id=#{@container_id}", :confirm => "Are you sure you want to delete this file?", :method => :delete, :class => "delete-button") %>'
	);
<% end %>
