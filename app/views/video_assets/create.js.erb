<%= remotipart_response do %>
	console.log('TADA');
	$('#uploader').html(
		'<legend>File Upload</legend>'
		+ '<a href="/<%= @video_asset.descMetadata.identifier[0]%>"><%= @video_asset.descMetadata.title[0]%></a>'
		+ '<span id="file_status"> (<%= @video_asset.descMetadata.description[0] %>)</span>'
		+ '<%= button_to("Delete", "/hydra/assets/#{@video_asset.pid}/file_assets/#{@video_asset.pid}?container_id=#{@container_id}", :confirm => "Are you sure you want to delete this file?", :method => :delete, :class => "delete-button") %>'
	);
<% end %>