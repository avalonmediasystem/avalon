<% # container for all documents in index view -%>
<div id="documents">
  <% if render_bookmarks_control? %> 
    <%= render "select_all" %>
  <% end %>
  <%= render documents, :as => :document %>
</div>
