<% if @response.total <= spell_check_max and @response.spelling.words.size > 0 %>
  <div id="spell" class="row">
    <h4 class="suggest">Did you mean <em><%= @response.spelling.words.map { |word| link_to_query(word) }.join(" #{t('blacklight.or')} ").html_safe %></em></h4>
  </div>
<% end %>
