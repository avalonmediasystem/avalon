    <div class="row-fluid whitespace">
      <div class="span12">
      <% unless @mediaobject.title.empty? %>
        <h2><%= @mediaobject.title.first %></h2>
      <% else %>
        <h2><%= @mediaobject.pid %></h2>
      <% end %>
     </div>
    </div>
    
    <div class="row-fluid whitespace">
      <section id="player">
        <h3>Associated file(s)</h3>
          <% unless @masterfiles.nil? %>
            <ul class="unstyled">
              <% @masterfiles.each do |masterfile| %>
                <li><a href="<%= master_file_path(masterfile) %>"><%= masterfile.label %></a> (<%= number_to_human_size(masterfile.size.first) %>)</li>
              <% end %>
            </ul>
            <% else %>
              <div class="well">
                <p>No files are associated with this resource yet.</p>
              </div>
            <% end %>
      </section>  

      <section id="metadata" class="whitespace">
        <h3>Metadata</h3>
         <%= render "modules/metadata_display" %>
      </section>
    </div>

  <div class="row-fluid">
    <div class="span8">
      <%= twitter_bootstrap_form_for @mediaobject, html: { class: "form-horizontal"} do |vid| %>
        <%= render "workflow_buttons", form: vid %>
      <% end %>
    </div>
  </div>
</div>
  
